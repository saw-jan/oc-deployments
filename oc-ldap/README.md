## ownCloud server with ldap

Server version: `owncloud/server:latest`

### Start the server

```bash
docker compose up
```

Server avaliable at `http://localhost:8080`

### Stop the server

```bash
docker compose down -v
```
